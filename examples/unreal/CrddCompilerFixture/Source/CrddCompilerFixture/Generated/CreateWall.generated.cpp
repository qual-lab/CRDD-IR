// Generated by crdd-ir. Do not edit.
// CRDD-TRACE: REQ-WALL
#include "CreateWall.generated.h"

#include <initializer_list>
#include <limits>

namespace
{
bool CrddTryAddInt64(int64 Left, int64 Right, int64& OutValue)
{
    if ((Right > 0 && Left > std::numeric_limits<int64>::max() - Right) ||
        (Right < 0 && Left < std::numeric_limits<int64>::min() - Right))
    {
        return false;
    }
    OutValue = Left + Right;
    return true;
}

bool CrddTrySubtractInt64(int64 Left, int64 Right, int64& OutValue)
{
    if ((Right < 0 && Left > std::numeric_limits<int64>::max() + Right) ||
        (Right > 0 && Left < std::numeric_limits<int64>::min() + Right))
    {
        return false;
    }
    OutValue = Left - Right;
    return true;
}

FCrddCreateWallResult Failure(
    ECrddCreateWallError Error,
    const TCHAR* FailedRequirement,
    const FCrddCreateWallState& InitialState,
    std::initializer_list<const TCHAR*> FailureTraces
)
{
    FCrddCreateWallResult Result;
    Result.Error = Error;
    Result.FailedRequirement = FailedRequirement;
    Result.State = InitialState;
    for (const TCHAR* Trace : FailureTraces)
    {
        Result.Traces.Add(Trace);
    }
    return Result;
}
}

FCrddCreateWallResult FCrddCreateWallOperation::Execute(
    const FCrddCreateWallInput& Input,
    const FCrddCreateWallState& InitialState
)
{
    // wall-id-required: input.wall_id != ""
    if (!((Input.WallId != TEXT(""))))
    {
        return Failure(
            ECrddCreateWallError::WallIdRequired,
            TEXT("wall-id-required"),
            InitialState,
            {TEXT("REQ-WALL")}
        );
    }

    // minimum-wall-length: input.length >= 300
    if (!((Input.Length >= 300)))
    {
        return Failure(
            ECrddCreateWallError::WallTooShort,
            TEXT("minimum-wall-length"),
            InitialState,
            {TEXT("REQ-WALL")}
        );
    }

    // minimum-wall-height: input.height >= 300
    if (!((Input.Height >= 300)))
    {
        return Failure(
            ECrddCreateWallError::WallTooLow,
            TEXT("minimum-wall-height"),
            InitialState,
            {TEXT("REQ-WALL")}
        );
    }

    // minimum-wall-thickness: input.thickness >= 10
    if (!((Input.Thickness >= 10)))
    {
        return Failure(
            ECrddCreateWallError::WallTooThin,
            TEXT("minimum-wall-thickness"),
            InitialState,
            {TEXT("REQ-WALL")}
        );
    }

    // opening-fits-width: input.opening_offset + input.opening_width <= input.length
    int64 CrddChecked4_0 = 0;
    const bool bCrddOverflow4_1 = !CrddTryAddInt64(Input.OpeningOffset, Input.OpeningWidth, CrddChecked4_0);
    if (bCrddOverflow4_1 || !((CrddChecked4_0 <= Input.Length)))
    {
        return Failure(
            ECrddCreateWallError::OpeningTooWide,
            TEXT("opening-fits-width"),
            InitialState,
            {TEXT("REQ-WALL")}
        );
    }

    // opening-fits-height: input.opening_sill + input.opening_height <= input.height
    int64 CrddChecked5_0 = 0;
    const bool bCrddOverflow5_1 = !CrddTryAddInt64(Input.OpeningSill, Input.OpeningHeight, CrddChecked5_0);
    if (bCrddOverflow5_1 || !((CrddChecked5_0 <= Input.Height)))
    {
        return Failure(
            ECrddCreateWallError::OpeningTooHigh,
            TEXT("opening-fits-height"),
            InitialState,
            {TEXT("REQ-WALL")}
        );
    }

    FCrddCreateWallResult Result;
    Result.bSucceeded = true;
    Result.State = InitialState;
    Result.State.Walls.Add({Input.WallId, Input.Length, Input.Height, Input.Thickness, Input.OpeningWidth, Input.OpeningHeight, Input.OpeningOffset, Input.OpeningSill});
    Result.Traces = {TEXT("REQ-WALL")};
    return Result;
}

FString FCrddCreateWallOperation::ErrorCode(ECrddCreateWallError Error)
{
    switch (Error)
    {
    case ECrddCreateWallError::None:
        return TEXT("");
    case ECrddCreateWallError::WallIdRequired:
        return TEXT("WALL_ID_REQUIRED");
    case ECrddCreateWallError::WallTooShort:
        return TEXT("WALL_TOO_SHORT");
    case ECrddCreateWallError::WallTooLow:
        return TEXT("WALL_TOO_LOW");
    case ECrddCreateWallError::WallTooThin:
        return TEXT("WALL_TOO_THIN");
    case ECrddCreateWallError::OpeningTooWide:
        return TEXT("OPENING_TOO_WIDE");
    case ECrddCreateWallError::OpeningTooHigh:
        return TEXT("OPENING_TOO_HIGH");
    default:
        return TEXT("UNKNOWN");
    }
}

bool FCrddCreateWallOperation::TryParseProjectedInt64(
    const FString& Decimal,
    int64& OutValue
)
{
    if (Decimal.IsEmpty() || Decimal.TrimStartAndEnd() != Decimal)
    {
        return false;
    }
    if (!LexTryParseString(OutValue, *Decimal))
    {
        return false;
    }
    return LexToString(OutValue) == Decimal;
}

FString FCrddCreateWallOperation::SerializeProjectedInt64(int64 Value)
{
    return LexToString(Value);
}
