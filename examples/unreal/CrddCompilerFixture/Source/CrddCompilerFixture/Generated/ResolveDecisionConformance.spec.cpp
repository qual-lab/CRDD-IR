// Generated by crdd-ir. Do not edit.
#if WITH_DEV_AUTOMATION_TESTS
#include "ResolveDecision.generated.h"
#include "Misc/AutomationTest.h"

IMPLEMENT_SIMPLE_AUTOMATION_TEST(
    FCrddResolveDecisionPortableConformanceTest,
    "CRDD.Conformance.ResolveDecision.Portable",
    EAutomationTestFlags::EditorContext | EAutomationTestFlags::EngineFilter
)

bool FCrddResolveDecisionPortableConformanceTest::RunTest(const FString&)
{
    {
        FCrddResolveDecisionInput Input;
        FCrddResolveDecisionState State;
        Input.Decision = TEXT("continue");
        Input.DangerGain = 0;
        State.Danger = 100;
        State.Status = TEXT("active");
        const FCrddResolveDecisionResult Result = FCrddResolveDecisionOperation::Execute(Input, State);
        TestTrue(TEXT("case 1: resolve-decision-success success"), Result.bSucceeded);
        TestEqual(
            TEXT("case 1: resolve-decision-success error"),
            FCrddResolveDecisionOperation::ErrorCode(Result.Error),
            TEXT("")
        );
        if (!Result.bSucceeded)
        {
            TestEqual(TEXT("case 1: rollback"), Result.FailedRequirement, TEXT(""));
        }
        TestTrue(TEXT("case 1: resolve-decision-success trace IR-CONDITIONAL-EFFECT-001"), Result.Traces.Contains(TEXT("IR-CONDITIONAL-EFFECT-001")));
        TestTrue(TEXT("case 1: resolve-decision-success trace BRANCH-CONTINUE"), Result.Traces.Contains(TEXT("BRANCH-CONTINUE")));
    }

    {
        FCrddResolveDecisionInput Input;
        FCrddResolveDecisionState State;
        Input.Decision = TEXT("continue");
        Input.DangerGain = 0;
        State.Danger = 100;
        State.Status = TEXT("active");
        const FCrddResolveDecisionResult Result = FCrddResolveDecisionOperation::Execute(Input, State);
        TestTrue(TEXT("case 2: resolve-decision-branch-decision-continue success"), Result.bSucceeded);
        TestEqual(
            TEXT("case 2: resolve-decision-branch-decision-continue error"),
            FCrddResolveDecisionOperation::ErrorCode(Result.Error),
            TEXT("")
        );
        if (!Result.bSucceeded)
        {
            TestEqual(TEXT("case 2: rollback"), Result.FailedRequirement, TEXT(""));
        }
        TestTrue(TEXT("case 2: resolve-decision-branch-decision-continue trace IR-CONDITIONAL-EFFECT-001"), Result.Traces.Contains(TEXT("IR-CONDITIONAL-EFFECT-001")));
        TestTrue(TEXT("case 2: resolve-decision-branch-decision-continue trace BRANCH-CONTINUE"), Result.Traces.Contains(TEXT("BRANCH-CONTINUE")));
    }

    {
        FCrddResolveDecisionInput Input;
        FCrddResolveDecisionState State;
        Input.Decision = TEXT("withdraw");
        Input.DangerGain = 0;
        State.Danger = 100;
        State.Status = TEXT("active");
        const FCrddResolveDecisionResult Result = FCrddResolveDecisionOperation::Execute(Input, State);
        TestTrue(TEXT("case 3: resolve-decision-branch-decision-withdraw success"), Result.bSucceeded);
        TestEqual(
            TEXT("case 3: resolve-decision-branch-decision-withdraw error"),
            FCrddResolveDecisionOperation::ErrorCode(Result.Error),
            TEXT("")
        );
        if (!Result.bSucceeded)
        {
            TestEqual(TEXT("case 3: rollback"), Result.FailedRequirement, TEXT(""));
        }
        TestTrue(TEXT("case 3: resolve-decision-branch-decision-withdraw trace IR-CONDITIONAL-EFFECT-001"), Result.Traces.Contains(TEXT("IR-CONDITIONAL-EFFECT-001")));
        TestTrue(TEXT("case 3: resolve-decision-branch-decision-withdraw trace BRANCH-WITHDRAW"), Result.Traces.Contains(TEXT("BRANCH-WITHDRAW")));
    }

    {
        FCrddResolveDecisionInput Input;
        FCrddResolveDecisionState State;
        Input.Decision = TEXT("continue");
        Input.DangerGain = 0;
        State.Danger = 100;
        State.Status = TEXT("active");
        const FCrddResolveDecisionResult Result = FCrddResolveDecisionOperation::Execute(Input, State);
        TestTrue(TEXT("case 4: danger-within-range-at-boundary success"), Result.bSucceeded);
        TestEqual(
            TEXT("case 4: danger-within-range-at-boundary error"),
            FCrddResolveDecisionOperation::ErrorCode(Result.Error),
            TEXT("")
        );
        if (!Result.bSucceeded)
        {
            TestEqual(TEXT("case 4: rollback"), Result.FailedRequirement, TEXT(""));
        }
        TestTrue(TEXT("case 4: danger-within-range-at-boundary trace IR-CONDITIONAL-EFFECT-001"), Result.Traces.Contains(TEXT("IR-CONDITIONAL-EFFECT-001")));
        TestTrue(TEXT("case 4: danger-within-range-at-boundary trace BRANCH-CONTINUE"), Result.Traces.Contains(TEXT("BRANCH-CONTINUE")));
    }

    {
        FCrddResolveDecisionInput Input;
        FCrddResolveDecisionState State;
        Input.Decision = TEXT("continue");
        Input.DangerGain = 0.001;
        State.Danger = 100;
        State.Status = TEXT("active");
        const FCrddResolveDecisionResult Result = FCrddResolveDecisionOperation::Execute(Input, State);
        TestFalse(TEXT("case 5: danger-within-range-outside-boundary success"), Result.bSucceeded);
        TestEqual(
            TEXT("case 5: danger-within-range-outside-boundary error"),
            FCrddResolveDecisionOperation::ErrorCode(Result.Error),
            TEXT("DANGER_LIMIT_EXCEEDED")
        );
        if (!Result.bSucceeded)
        {
            TestEqual(TEXT("case 5: rollback"), Result.FailedRequirement, TEXT("danger-within-range"));
        }
        TestTrue(TEXT("case 5: danger-within-range-outside-boundary trace IR-CONDITIONAL-EFFECT-001"), Result.Traces.Contains(TEXT("IR-CONDITIONAL-EFFECT-001")));
    }

    {
        FCrddResolveDecisionInput Input;
        FCrddResolveDecisionState State;
        Input.Decision = TEXT("continue");
        Input.DangerGain = 0;
        State.Danger = 99.999;
        State.Status = TEXT("active");
        const FCrddResolveDecisionResult Result = FCrddResolveDecisionOperation::Execute(Input, State);
        TestTrue(TEXT("case 6: danger-within-range-inside-boundary success"), Result.bSucceeded);
        TestEqual(
            TEXT("case 6: danger-within-range-inside-boundary error"),
            FCrddResolveDecisionOperation::ErrorCode(Result.Error),
            TEXT("")
        );
        if (!Result.bSucceeded)
        {
            TestEqual(TEXT("case 6: rollback"), Result.FailedRequirement, TEXT(""));
        }
        TestTrue(TEXT("case 6: danger-within-range-inside-boundary trace IR-CONDITIONAL-EFFECT-001"), Result.Traces.Contains(TEXT("IR-CONDITIONAL-EFFECT-001")));
        TestTrue(TEXT("case 6: danger-within-range-inside-boundary trace BRANCH-CONTINUE"), Result.Traces.Contains(TEXT("BRANCH-CONTINUE")));
    }

    {
        FCrddResolveDecisionInput Input;
        FCrddResolveDecisionState State;
        Input.Decision = TEXT("withdraw");
        Input.DangerGain = 0;
        State.Danger = 100;
        State.Status = TEXT("withdrawn");
        const FCrddResolveDecisionResult Result = FCrddResolveDecisionOperation::Execute(Input, State);
        TestFalse(TEXT("case 7: withdraw-only-while-active-falsified success"), Result.bSucceeded);
        TestEqual(
            TEXT("case 7: withdraw-only-while-active-falsified error"),
            FCrddResolveDecisionOperation::ErrorCode(Result.Error),
            TEXT("STATUS_NOT_ACTIVE")
        );
        if (!Result.bSucceeded)
        {
            TestEqual(TEXT("case 7: rollback"), Result.FailedRequirement, TEXT("withdraw-only-while-active"));
        }
        TestTrue(TEXT("case 7: withdraw-only-while-active-falsified trace BRANCH-WITHDRAW"), Result.Traces.Contains(TEXT("BRANCH-WITHDRAW")));
    }

    return true;
}
#endif
