{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://crdd.dev/schema/ir/v0.1.json",
  "title": "CRDD IR v0.1",
  "type": "object",
  "required": ["irVersion", "operation"],
  "properties": {
    "irVersion": { "const": "0.1" },
    "operation": {
      "type": "object",
      "required": ["id", "kind", "traces", "input", "state", "requires", "effects", "errors"],
      "properties": {
        "id": { "type": "string", "minLength": 1 },
        "kind": { "enum": ["command", "query"] },
        "traces": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "input": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/field" }
        },
        "state": {
          "type": "object",
          "additionalProperties": { "$ref": "#/$defs/field" }
        },
        "output": { "$ref": "#/$defs/field" },
        "requires": { "type": "array" },
        "effects": {
          "type": "array",
          "items": { "$ref": "#/$defs/effect" }
        },
        "errors": { "type": "array" },
        "execution": { "$ref": "#/$defs/execution" },
        "emits": {
          "type": "array",
          "items": { "$ref": "#/$defs/event" }
        },
        "extensions": {
          "type": "object",
          "propertyNames": { "pattern": "^[a-z][a-z0-9.-]*$" },
          "additionalProperties": { "$ref": "#/$defs/extension" }
        },
        "transaction": {
          "type": "object",
          "required": ["atomic", "rollbackOnFailure"],
          "properties": {
            "atomic": { "type": "boolean" },
            "rollbackOnFailure": { "type": "boolean" }
          },
          "additionalProperties": false
        }
      }
    }
  },
  "$defs": {
    "extension": {
      "type": "object",
      "required": ["protocol", "data"],
      "properties": {
        "protocol": { "type": "string", "minLength": 1 },
        "data": {}
      },
      "additionalProperties": false
    },
    "effect": {
      "oneOf": [
        {
          "type": "object",
          "required": ["target", "action", "expression"],
          "properties": {
            "target": { "type": "string", "minLength": 1 },
            "action": { "enum": ["assign", "increment"] },
            "expression": { "type": "string", "minLength": 1 }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["target", "action", "value"],
          "properties": {
            "target": { "type": "string", "minLength": 1 },
            "action": { "const": "append" },
            "value": {}
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["target", "action", "where"],
          "properties": {
            "target": { "type": "string", "minLength": 1 },
            "action": { "const": "remove" },
            "where": { "type": "object", "minProperties": 1 }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["target", "action", "where", "set"],
          "properties": {
            "target": { "type": "string", "minLength": 1 },
            "action": { "const": "update" },
            "where": { "type": "object", "minProperties": 1 },
            "set": { "type": "object", "minProperties": 1 }
          },
          "additionalProperties": false
        }
      ]
    },
    "scalar": {
      "type": "object",
      "required": ["type"],
      "properties": {
        "type": { "enum": ["number", "integer", "string", "boolean"] },
        "unit": { "type": "string", "minLength": 1 },
        "minimum": { "type": "number" },
        "maximum": { "type": "number" },
        "minLength": { "type": "integer", "minimum": 0 },
        "maxLength": { "type": "integer", "minimum": 0 },
        "pattern": { "type": "string" },
        "enum": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": { "type": "string", "minLength": 1 }
        },
        "optional": { "type": "boolean" },
        "nullable": { "type": "boolean" },
        "default": { "type": ["number", "string", "boolean"] }
      },
      "additionalProperties": false
    },
    "object": {
      "type": "object",
      "required": ["type", "properties"],
      "properties": {
        "type": { "const": "object" },
        "properties": {
          "type": "object",
          "minProperties": 1,
          "additionalProperties": { "$ref": "#/$defs/field" }
        },
        "optional": { "type": "boolean" },
        "nullable": { "type": "boolean" }
      },
      "additionalProperties": false
    },
    "field": {
      "oneOf": [
        { "$ref": "#/$defs/scalar" },
        { "$ref": "#/$defs/object" },
        {
          "type": "object",
          "required": ["type", "items"],
          "properties": {
            "type": { "const": "array" },
            "items": { "$ref": "#/$defs/field" },
            "minItems": { "type": "integer", "minimum": 0 },
            "maxItems": { "type": "integer", "minimum": 0 },
            "optional": { "type": "boolean" },
            "nullable": { "type": "boolean" }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["type", "values"],
          "properties": {
            "type": { "const": "map" },
            "values": { "$ref": "#/$defs/field" },
            "optional": { "type": "boolean" },
            "nullable": { "type": "boolean" }
          },
          "additionalProperties": false
        },
        {
          "type": "object",
          "required": ["type", "discriminator", "variants"],
          "properties": {
            "type": { "const": "union" },
            "discriminator": { "type": "string", "minLength": 1 },
            "variants": {
              "type": "array",
              "minItems": 2,
              "items": { "$ref": "#/$defs/object" }
            },
            "optional": { "type": "boolean" },
            "nullable": { "type": "boolean" }
          },
          "additionalProperties": false
        }
      ]
    },
    "execution": {
      "type": "object",
      "required": ["mode"],
      "properties": {
        "mode": { "enum": ["sync", "async"] },
        "cancelable": { "type": "boolean" },
        "timeoutMs": { "type": "integer", "exclusiveMinimum": 0 },
        "idempotency": { "enum": ["none", "optional", "required"] }
      },
      "additionalProperties": false
    },
    "event": {
      "type": "object",
      "required": ["type", "traces"],
      "properties": {
        "type": { "type": "string", "minLength": 1 },
        "payload": { "$ref": "#/$defs/field" },
        "delivery": { "enum": ["at-most-once", "at-least-once"] },
        "traces": { "$ref": "#/$defs/traces" }
      },
      "additionalProperties": false
    },
    "traces": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "type": "string", "minLength": 1 }
    }
  },
  "additionalProperties": false
}
